# ACL 2027 Phase 3A real-task mechanism audit v1

## Scope

This is a zero-network descriptive audit of the completed Phase 2 v31 grid. It does not rescore or re-gate Phase 2, create an authorization, or make provider/model/paid calls.

## Observable uptake

Among 400 tasks, all four raw responses were identical for `294` tasks and all normalized answers were identical for `340`. Alias-tolerant outcomes were equal across all conditions for `368` tasks. Typed and global-only raw responses differed for only `53` tasks.

The frozen taxonomy counts are `{'response_change_without_alias_outcome_change': 74, 'no_observable_uptake': 294, 'other_outcome_heterogeneity': 15, 'typed_benefit': 11, 'typed_harm': 6}`. The alias-tolerant global-degradation stratum contains `9` tasks; typed recovered `3` and copied-global recovered `0`.

## Decision

Random same-distribution scale-up decision: **reject_random_scale_up**. A mechanism-bridge experiment is required: `true`. The next admissible stage is a zero-network Phase 3B activation preflight for 60 new tasks and five conditions (300 proposed calls), with structured applicability, skill-ID, intermediate-operation, and final-answer observations. That proposal is not materialized and not authorized.

## Integrity

Aggregate fingerprint: `0b2c420671b02b4210c3903d5549c5ec137699ec501c028a949da4ebb6212e3c`. Phase 2 gate remains `inconclusive`. Network/provider/model/paid/formal-scaling counters: `0/0/0/0/0`.
