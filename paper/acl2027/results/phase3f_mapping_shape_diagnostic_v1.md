# ACL 2027 Phase 3F boolean-mapping diagnostic v1

This diagnostic accepts direct boolean mapping values in addition to the frozen nested-object values. It is descriptive only and cannot change the strict Phase 3F gate.

The explicitly authorized execution completed `240/240` calls with `238/240` contract-valid responses.

Contextual-single correct selection was `40/40` (`1.0000`); irrelevant-single rejection was `40/40` (`1.0000`). Dual correct selection was `78/80` (`0.9750`), dual wrong selection was `0/80` (`0.0000`), and the absolute order effect was `0.0500`.

Contextual-versus-irrelevant operation changes were `29/40`; normalized-answer changes were `2/40`. Alias-tolerant contextual net wins over irrelevant were `2`, with contextual net loss versus cold `1`.

Frozen decision gate: **non_gating**. Cross-domain scaling allowed: `False`.

Exact stage cost: CNY `1.543848`. Known cumulative cost lower bound: CNY `10.334338`. Authorization is closed; later stages and formal scaling remain unauthorized.

Aggregate fingerprint: `96e6678979361ed7b840bc0b1e80ac4ddbf1b19ebbb49ede377a3379b224d0b4`.
