# ACL 2027 Phase 3D specificity and abstention live v3

The explicitly authorized execution completed `240/240` calls with `0/240` contract-valid responses.

Contextual-single correct selection was `0/40` (`0.0000`); irrelevant-single rejection was `0/40` (`0.0000`). Dual correct selection was `0/80` (`0.0000`), dual wrong selection was `0/80` (`0.0000`), and the absolute order effect was `0.0000`.

Contextual-versus-irrelevant operation changes were `0/40`; normalized-answer changes were `0/40`. Alias-tolerant contextual net wins over irrelevant were `0`, with contextual net loss versus cold `0`.

Frozen decision gate: **negative**. Cross-domain scaling allowed: `False`.

Exact stage cost: CNY `1.571752`. Known cumulative cost lower bound: CNY `8.79049`. Authorization is closed; later stages and formal scaling remain unauthorized.

Aggregate fingerprint: `501f57df97267d3dae84fa953e0652102e5d003572d9e40c58d8a50010e22b29`.
