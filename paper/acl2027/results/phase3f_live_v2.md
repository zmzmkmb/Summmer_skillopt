# ACL 2027 Phase 3F contract-control live v2

The explicitly authorized execution completed `240/240` calls with `221/240` contract-valid responses.

Contextual-single correct selection was `40/40` (`1.0000`); irrelevant-single rejection was `25/40` (`0.6250`). Dual correct selection was `76/80` (`0.9500`), dual wrong selection was `0/80` (`0.0000`), and the absolute order effect was `0.1000`.

Contextual-versus-irrelevant operation changes were `32/40`; normalized-answer changes were `16/40`. Alias-tolerant contextual net wins over irrelevant were `15`, with contextual net loss versus cold `1`.

Frozen decision gate: **inconclusive**. Cross-domain scaling allowed: `False`.

Exact stage cost: CNY `1.543848`. Known cumulative cost lower bound: CNY `10.334338`. Authorization is closed; later stages and formal scaling remain unauthorized.

Aggregate fingerprint: `d797e795ba5d6318cbeb01ce8ff7187949a134679a621a673855b216e3a3d0cc`.
